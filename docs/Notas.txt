-Modulos y funciones: Añadir descripción, autoria, fecha de cracion de cada módulo, descripcion de input y output
-Esquema interrelación modulos y funciones en pdf
-Dividir repositorio en codigo fuente, aplicacion portable, instalador y documentacion
-Verificar entradas, por ejemplo no poder introducir texto donde se requiere un valor numerico
-Permitir introducir nuevos codecs, primero hacerlo mediante un tabla de office y luego permitiendo modificar esa tabla en apartado settings
-Permitir en apartado settings poner envio a correo por defecto y al final de proceso
-Permitir volver hacia atras en el proceso de calculo

-Permitir todas las combinaciones de los valores de los anexos, tipos de codecs, tipos de encapsulación, etc
-No permitir que se introduzca retardo total (oido-oreja) ni opinion subjetiva de calidad de llamada (MOS) ni nivel de satisfacción de forma numérica.
-Realizar aplicación por pasos con boton de atras y continuar
-Aplicación interfaz en negro
-Añadir boton ayuda, info(autores, parametros, simplifcaciones que tenga en cuenta la app y settings)
-Añadir informacion de cada parametro al poner el ratón o pinchar en un ? al lado de cada parámetro

DATOS DE ENTRADA:
-Número empresas cliente (Nc):
-Numero de lineas por cliente (Nl).
-Tiempo medio de llamda (Tll).
-Probabilidad llamada (Pll). (Por defecto 100%)
-Conxión WAN (Tcwan): tipo encapsulacion entre empresa VoIP4ALL y PROVOIP en la conexión del SIPTRUNK
-Ancho de banda de reserva BWres(%)
-El diseño no contempla el uso de VAD
QoE y QoS
-MOS 
-Rt
-Rr
-J
-Considerar RTP y CRTP
-COnsiderar retardos por codecs y paquetizacion, no serialización. En destinto considrar retardo por descodificacion como 10% del tiempo de codificación de bloque/trama de audio
GoS
-Prob bloqueo Pb
-Anco de band SIPTRUNK BWst


PASOS QUE DEBE SEGUIR APP
1.- Esquema
2.- QoE -> La app elige un codec según MOS
3.- QoS -> Para ese codec calcula y muestra Rt teniendo en cuenta:
	

REALIZAR TRELLO
DUDAS
-Como especificar MOS y Retardo Maximo Permitido

BUGS: programa permite avanzar aunque haya parametros en blanco y no permite retroceder
CAMBIAR NOMBRE VENTANA